<% for (let referrer of model.referencedIn.filter(m => m.accesses.properties.onlyGuest || m.accesses.properties.noAuth)) { -%>
    <% for (let field of referrer.fields) { -%>
await db.collection('<%= referrer.names.pascal %>').deleteMany({ <%= field.names.snake %>: id });
   <% } -%>
<% } -%>